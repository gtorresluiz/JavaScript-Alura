const nomes = ["Ana", "Clara", "Maria", "Maria", "João", "João", "João"];

const nomesAtualizados = [...new Set[nomes]]

console.log(nomesAtualizados)

// sets possuem métodos bem diferentes de arrays, entre eles, a não repetição de elementos

// por exemplo, length = Set.prototype.size
